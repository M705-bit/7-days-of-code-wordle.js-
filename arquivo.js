const letras = document.querySelectorAll(".flex-item");
const botao 
for (let letra = 0; letra < letras.length; letra++) {
	    letras[0].textContent;
		//alvo.addEventListener(type,listener[, option]);
		letras.addEventListener("click", findWord());
}
 alert(letras[0].textContent);

 function findWord(){
	if l
 }
